// o_loot_manager — Create Event
// Quản lý việc tương tác loot từ xác Enemy.
// Object nên set Persistent = true.

// Khoảng cách để nhặt đồ từ xác
PICKUP_RANGE = 80;

// Xác gần nhất đang trong range của Player (noone nếu không có)
closest_corpse = noone;
